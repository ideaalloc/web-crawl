# web-crawl

### Priority

Python 2.7.*

### Configure it

```
$ pip install scrapy
```

### Run it

```
$ cd web-crawl
$ scrapy crawl products -o products.json
$ scrapy crawl details -o details.json
```